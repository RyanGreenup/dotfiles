return {
	{ "nvim-lua/plenary.nvim", lazy = true },
	-- { "nvim-tree/nvim-web-devicons", lazy = true }, -- Required by Neotree [fn_req]
	{ "echasnovski/mini.icons", lazy = false },
}

-- Footnotes
-- [fn_req]
-- Dependencies should only be specified if they must **also**
-- be loaded
